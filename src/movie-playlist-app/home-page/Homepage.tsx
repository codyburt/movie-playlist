import SearchBar from "../common/SearchBar"

const Homepage = (): JSX.Element => {
  return (
    <div>
      <h1>Create your own movie playlist!</h1>
      <SearchBar />
    </div>
  )
}

export default Homepage
